# Claude Code Adapter

> This is a platform-specific adapter. The canonical instructions
> live in `KERNEL.md`. Read that file first.

This file exists because Claude Code auto-discovers CLAUDE.md at
project root. Its purpose is routing, not authority.

## Instructions

1. Read `KERNEL.md` — the canonical agent instruction surface.
2. If this is the first session, read `ONBOARD.md`.
3. Read `kernel/` — five foundational positions.
4. Read `trunk/` — twelve operational protocols (reference as needed).
5. Read `alpha_entity.md` — the sovereign human you serve.
6. Browse `harvests/` for structural context when stuck or exploring.

## Claude-Specific Notes

- Use file-based commit messages (`git commit -F`) for cross-platform
  compatibility with cmd.exe / PowerShell / bash.
- The `#kernel+` tag in FADE documents marks atoms that pass the veil
  of ignorance test (domain-independent insights).
- Platform adapter files (this file, .cursorrules, etc.) are
  emanations of KERNEL.md. If they conflict, KERNEL.md wins.
- The virtue journey is a standing obligation — surface meaningful
  decision forks during non-trivial work, don't wait to be asked.
