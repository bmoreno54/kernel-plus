# Pidgin: Paradigm Walking Sim as Thermal Contract in 3D

**Source:** hearth (paradigm.html 3D body plan integration)
**Date:** 2026-04-25
**Type:** convergence confirmation

The MTG paradigm walking simulator now implements the full thermal rendering contract in navigable 3D space:

- **Category** = body plan type (18 procedural geometry types)
- **Intensity** = scale (derived from card properties)
- **DOI** = hook score (tag affinity + body plan biome bonus)
- **Zone** = euclidean player distance (proxemic rendering)

This is the sixth independent surface computing the same four-axis decomposition. The 3D implementation reveals that Hall's proxemic zones are literal when the rendering surface is spatial — the sigmoid falloff at zone boundaries operates on camera-to-entity distance rather than tree depth or scroll position.

The hook system and IE's substrate.js are convergent implementations of the same tag-intensity substrate. The walking sim could be rebuilt on substrate.js without architectural change.

37,236 cards verified rendering as embodied 3D creatures across 5 biomes. Zero console errors. Body plan affinity clustering confirmed (angels in Plains, treefolk in Forests, spectrals in Swamps).
