# schema — what the graph has discovered about itself

> this is emergent documentation, not prescription. it records
> what the nodes have become, not what they should be.
> the foundational positions live in [node.md](node.md),
> [relation.md](relation.md), and [provenance.md](provenance.md).
> their molecular composition is in [node_schema.md](node_schema.md).

## node types (observed)

these names emerge from what the nodes actually are. they are
not categories to instantiate — they are vocabulary the graph
gives itself.

| type | what it turned out to be | instances |
|---|---|---|
| foundational | minimal ontomechanical position | node.md, relation.md, provenance.md |
| composition | molecular cluster of foundational positions | node_schema.md |
| identity | sovereign entity at ecosystem center | alpha_entity.md |
| meta | what the graph discovered about itself | this file |

> new types accrete as the graph encounters them. this table
> grows from use, not from planning.

## edge types (observed)

### structural (functional wiring)

| type | meaning | observed in |
|---|---|---|
| composes | molecularly constituted from | node_schema.md → node.md, relation.md, provenance.md |
| creates | originates from | alpha_entity.md → (creative artifacts) |

> new edge types accrete as the graph encounters new kinds of
> connection. the vocabulary is provisional.

### other modalities (non-exhaustive)

| modality | what it is |
|---|---|
| narrative | thematic resonance between nodes |
| temporal | proximity in creation or activation |
| semantic | shared concepts across different domains |
| synesthetic | cross-modal impressions |

these modalities are not lesser relations. they operate in different
registers. contextualizing the modality makes them traversable.
new modalities accrete as the graph encounters new kinds of connection.

## what is deferred

the types and edges above are observed vocabulary, not a closed
taxonomy. new types accrete as the graph encounters them.

what is invariant: the sovereignty test (from [node.md](node.md)),
the intersubjective test (from [relation.md](relation.md)), and
provenance as structural fact (from [provenance.md](provenance.md)).
these are the load-bearing structure. everything else is provisional.
